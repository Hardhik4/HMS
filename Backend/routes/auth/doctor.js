import { Router } from "express";

const router = Router();

router.get("/login", (req, res) => {
    /* login a doctor */
});

router.get("/register", (req, res) => {
    /* register a doctor */
});

export default router;
