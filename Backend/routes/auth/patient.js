import { Router } from "express";

const router = Router();

router.get("/login", (req, res) => {
    /* login a patient */
});

router.get("/register", (req, res) => {
    /* register a patient */
});

export default router;
